// REMOVED: nodemailer-based email utilities are not used in Cloudflare Workers deployment.
// Use SendGrid or a Workers-compatible email API from the Worker directly if needed.

module.exports = {};
